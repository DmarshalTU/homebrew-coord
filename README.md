# homebrew-coord

Homebrew tap for [`coord`](https://github.com/DmarshalTU/coord) — a local
coordinator for parallel AI coding agents.

## Install

```bash
brew tap dmarshaltu/coord
brew install coord
```
